<div>
    <a href = "/families/<%= family.slug %>/<%= member.slug %>">
        <%= member.member.firstname %> <%= member.member.lastname %>
    </a>

    <form
        action = "/families/<%= family._id %>/<%= member._id %>/delete?_method=DELETE"
        method = "POST"
    >
        <button type = "submit">Delete</button>
    </form>
</div>