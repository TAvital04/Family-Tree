<%- include("../partials/header.ejs", {title}) %>

<h1><%= title %></h1>

<a href = <%= `${req.params.familySlug}/addToRoot` %>>Add Member</a>

<% if(members) { 
    members.forEach(member => { 
        if(member) { %>
            <%- include("../components/listMember.ejs", {req, member}) %>
        <% } 
    })
} %>

<%- include("../partials/footer.ejs", {scripts: undefined}) %>