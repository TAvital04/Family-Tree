<%- include("../partials/header.ejs", {title, styles: ["query.css"]}) %>

<div class = "log">
    <h1>Query</h1>

    <% if(members) { 
        members.forEach(member => { 
            if(member) { %>
                <%- include("../components/listMember.ejs", {req, member}) %>
            <% } 
        })
    } %>

    <br>

    <a href = <%= `/families/${req.params.familyTarget}` %>>
        <button>Back to Family</button>
    </a>    
</div>


<%- include("../partials/footer.ejs", {scripts: undefined}) %>